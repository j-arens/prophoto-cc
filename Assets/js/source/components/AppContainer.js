// @flow
import { connect } from 'react-redux';
import App from './App';

const mapStateToProps = (state: State) => ({

});

const mapDispatchToProps = (dispatch: (any) => mixed) => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(App);
